
mutation updateSongDatabase($params: UpdateSongDatabaseInput) {
	updateSongDatabase(params: $params) {
		songDatabase {
			id 
			name,
			variations {
				id
				name
			}
			tags {
				id
			}
		}
	}
}