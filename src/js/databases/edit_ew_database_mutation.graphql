
mutation updateEwDatabase($params: UpdateEwDatabaseInput) {
	updateEwDatabase(params: $params) {
		ewDatabase {
			id 
			name
			databaseKey
			songDatabaseId
			removeSongsFromExternalDatabase
			removeSongsFromSongDatabase
			variationVersionConflictAction
		}
	}
}